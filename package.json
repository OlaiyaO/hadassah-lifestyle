{
  "name": "hadassah-lifestyle",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run products:sync && next dev",
    "build": "npm run products:sync && next build",
    "build:netlify": "npm run products:sync && next build",
    "start": "next start",
    "lint": "eslint .",
    "test": "node --test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "products:sync": "node scripts/sync-products.mjs",
    "products:check": "node scripts/sync-products.mjs --check"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "next": "16.2.10",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "csv-parse": "^7.0.1",
    "eslint": "^9",
    "eslint-config-next": "16.2.10",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.5",
    "tailwindcss": "^4"
  }
}
